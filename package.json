{
  "name": "root",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build --stream",
    "lint": "lerna run lint --stream --parallel"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.6.2",
    "eslint": "7.7.0"
  }
}
